---
import { flex } from 'styled-system/patterns'

interface Props {
  type?: string
}
const { type } = Astro.props

function getIcon() {
  switch (type) {
    case "success":
      return "✅"
    default:
      return "💡"
  }
}

const root = flex({
  alignItems: "center",
  w: "full",
  p: 4,
  gap: 4,
  border: "primary",
  borderRadius: "box",

  "& > *": {
    m: 0,
  }
})

---

<div class={root}>
  <p>{getIcon()}</p>
  <slot />
</div>

<style>
  .callout {
    display: inline-flex;
    gap: .5rem;
    width: 100%;
    border: 1px black solid;
  }
</style>
